# 🚀 **STEP 1 — S3 Bucket Create karo**

AWS Console → Search **S3** → Open

### Click: **Create bucket**

Fill like this:

| Field               | Value                                                    |
| ------------------- | -------------------------------------------------------- |
| Bucket Name         | `student-project-tracker-frontend` (unique hoga tumhara) |
| AWS Region          | `ap-south-1` (Mumbai)                                    |
| Block Public Access | **Uncheck** ❗                                            |
| Confirm checkbox    | ✔ Allow public access                                    |

Then click **Create bucket**.

---

# 🚀 **STEP 2 — Bucket ko Public Website bana do**

Bucket → **Permissions** tab → Scroll → **Bucket Policy** → Edit

Paste this policy (bucket-name replace karna mat bhoolna):

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "PublicReadAccess",
      "Effect": "Allow",
      "Principal": "*",
      "Action": [
        "s3:GetObject"
      ],
      "Resource": [
        "arn:aws:s3:::student-project-tracker-frontend/*"
      ]
    }
  ]
}
```

Save changes.

---

# 🚀 **STEP 3 — Static Website Hosting Enable karo**

Bucket → **Properties** → Scroll down → **Static Website Hosting**

→ Click **Edit**

Set:

| Setting        | Value                   |
| -------------- | ----------------------- |
| Static hosting | Enable                  |
| Index Document | `index.html`            |
| Error Document | `index.html` (optional) |

SAVE.

Now AWS will give you a public website URL:

👉 Example:

```
http://student-project-tracker-frontend.s3-website.ap-south-1.amazonaws.com
```

Ye tumhare frontend ka LIVE PUBLIC URL hoga.

---

# 🚀 **STEP 4 — Frontend Upload karo**

Bucket → **Objects** tab → Upload → Entire `frontend/` folder ka content upload karo.

**IMPORTANT:**

* Sirf *folder ke andar ke files* upload karo
* NOT the folder itself

Example:

```
index.html
assets/css/style.css
assets/js/*.js
student/dashboard.html
mentor/dashboard.html
```

Upload → Make public (if option comes).

---

# 🚀 **STEP 5 — config.js me EC2 backend URL add karo**

Jab tumhara backend EC2 par deploy ho jayega, tum **config.js** me sirf base URL change karoge:

👉 Path:

```
frontend/assets/js/config.js
```

### LOCAL:

```javascript
export const API_BASE_URL = "http://localhost:3000/api";
```

### EC2 DEPLOY:

```javascript
export const API_BASE_URL = "http://YOUR-EC2-IP:3000/api";
```

Bas.
Pure frontend automatically EC2 backend se connect ho jayega.

---

# 🚀 **STEP 6 — LIVE TESTING**

Ab open karo:

```
http://your-bucket-name.s3-website.ap-south-1.amazonaws.com
```

Aur tumhari HTML website **live** hogi.

Dashboard → Projects → Tasks sab chalega.

---

# 🔥 BONUS: Folder Auto-Upload Script (Future use)

Jab tum GitHub → CodePull automation karoge tab ye shell script helpful hogi:

```bash
aws s3 sync frontend/ s3://student-project-tracker-frontend/ --delete
```

Ye command poora frontend **auto-upload** kar dega.

---

# 🎉 FINAL RESULT

Tumhara **FULL FRONTEND WEBSITE live ho jayega AWS S3 par**:

✔ No server required
✔ Free static hosting
✔ Public URL milta hai
✔ Backend EC2 se connect karta hai
✔ Dashboard + Projects + Tasks sab run hoga

---
